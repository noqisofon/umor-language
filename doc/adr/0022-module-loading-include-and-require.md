# ADR-0022: モジュール読み込み機構 — `含める`と`必要`

## Status

Proposed（ふわっと合意。詳細は宿題）

## Context

Umorにはまだファイル分割・ライブラリ読み込みの機構がない。
将来のAGC（Adventure Game Construction）構想や、curses的な
TUIバインディングをはじめとする「バンドルライブラリ」の
読み込みを見据えると、モジュールシステムがどこかの時点で
必要になる。

Forth系言語の伝統的な語彙である`INCLUDE`（無条件に毎回読み込む）
と`REQUIRE`（未読み込みならロード、読み込み済みならスキップする
一度きり保証）に相当する日本語ワードとして、以下が提案された。

```
「./honi.umor」を 含める。
「./honi.umor」が 必要。
```

助詞の使い分け（`を`／`が`）は、`含める`が能動的な操作
（対象を取り込む）であるのに対し、`必要`が状態の要求
（これが要る、という宣言）である点と対応しており、
日本語話者の直感とも合っている。

## Decision

以下の2ワードをモジュール読み込み機構の入り口として導入する。

- **`「path」を 含める`**：対象ファイルを無条件に読み込んで
  その場で評価する。Forthの`INCLUDE`相当。
- **`「path」が 必要`**：対象ファイルが未読み込みであれば
  読み込み、読み込み済みであればスキップする。Forthの
  `REQUIRE`相当。

現時点では両ワードの存在と大まかな意味論のみを決定する。
実装の詳細判断は次の「宿題」として保留する。

## 宿題（未解決事項）

- **`必要`の重複読み込み判定方法**：どのように「読み込み済み」を
  管理するか（辞書に読み込み済みパスの一覧を持たせるか、
  別途レジストリを設けるか）。
- **パス正規化**：`./honi.umor`と`honi.umor`のような表記ゆれを
  同一ファイルとして扱うかどうか、扱うならどう正規化するか。
- **相対パスの基準点**：実行中のスクリプトファイルからの相対か、
  プロセスのカレントディレクトリからの相対か。
- **将来のバンドルライブラリ参照との関係**：`"curses"`のような
  ライブラリ名を`含める`／`必要`にそのまま渡せるようにするのか、
  ファイルパスとライブラリ名で別の解決ロジックを用意するのか。

これらは本ADRの更新、または後続ADRで別途扱う。

## Consequences

**得られるもの**：

- ファイル分割・ライブラリ読み込みという、AGC構想やバンドル
  ライブラリ構想の前提となる機構に、最小限の語彙的な出発点が
  与えられる。
- `含める`（毎回評価）と`必要`（一度きり保証）という意味論の
  違いをワードレベルで最初から区別しておくことで、後から
  「重複読み込み時の挙動をどうするか」で揉める余地を減らせる。

**トレードオフとして受け入れるもの**：

- 重複防止の実装詳細が未確定のため、`必要`は現時点では
  仕様として不完全である。実装に着手する際は本ADRの宿題を
  先に解決する必要がある。
- モジュールシステムとしての全体設計（名前空間分離をするか、
  読み込んだワードがグローバル辞書にそのまま追記されるのか等）
  には踏み込んでいない。今回は`含める`・`必要`という2つの
  ワードの導入のみを扱う、意図的に薄いADRである。

## References

- 契機となった議論：AGC（Adventure Game Construction）構想と、
  curses的なTUIバインディングを「バンドルライブラリ」として
  同梱する構想から、モジュール読み込み機構の必要性が浮上した
  こと（2026-09-08）
- 参照言語：Forthの`INCLUDE`／`REQUIRE`の伝統的な意味論
- 関連：ADR-0014（基本ワード／標準ライブラリ境界の分離。
  バンドルライブラリの位置づけを検討する際に参照される見込み）
